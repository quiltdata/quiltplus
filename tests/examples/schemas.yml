QuiltPlusURI:
  registry: str # uri
  ?package: str # require "/"
  ?top_hash: str # base64
  ?path: str 
  ?catalog: str # uri
  ?property: dict
QuiltPlusPackage:
  prefix: str
  suffix: str
  ?top_hash: str # base64
QuiltPlusPath:
  paths: "list[str]"
  ?absolute: bool=True
